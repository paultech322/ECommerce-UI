import ClipSpan from "../../../ClipSpan"

const StartIcon = ({ label }) => (
  <ClipSpan className="md:text-[10.8px] lg:text-[14.4px] xl:text-[18px] font-urwgeometric_bold pt-[4px]">
    {label}
  </ClipSpan>
)

export default StartIcon
